// Inicializar una variable con un valor
var producto = 'Monitor de 24 pulgadas';
console.log(producto);

// Las variables se pueden reasignar
producto = 'Monitor de 19 pulgadas';
console.log(producto);

// Javascript es un lenguaje de tipo dinamico, no se especifica tipo de dato
producto = 29;
console.log(producto);

// Se pueden inicializar varibales son valor y asignarlo despues
var disponible;

disponible = true;

disponible = false;

// Inicializar múltiples variables.
var categoria = 'Computadoras',
  marca = 'Marca famosa',
  calificacion = 5;

//   Las variables no pueden iniciar con numeros, pero los nombres si pueden tener numeros

var nombreProducto = 'Computadora';
