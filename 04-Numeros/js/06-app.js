// Metodos numericos

const numero1 = '20';

const numero2 = '20.2';

const numero3 = 'Uno';

const numero4 = 20;

// Convertir un string en numero entero
console.log(Number.parseInt(numero1));

// Convertir un string en un numero flotante
console.log(Number.parseFloat(numero2));

// Revisar si en numero es entero o no
console.log(Number.isInteger(numero4));
